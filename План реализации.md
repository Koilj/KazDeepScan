# KazDeepScan — план реализации и дальнейшие действия

**Актуально:** 15 августа 2026

**Текущий scope:** tagged KazDeepScan v1.0 Research и отдельный post-v1.0 local user inference
v1; personal research без сбора голосов людей, voice cloning и product/API risk score.

Этот файл является дорожной картой. Подробная архитектура находится в
`KazDeepScan_implementation_blueprint.md`, короткий фактологический статус — в
`PROJECT_STATUS.md`, новые подробные результаты и receipts — в `docs/artifacts/`.

Активная development-ветка описана отдельно в
[плане XLS-R+SLS model v4](docs/kazdeepscan_v4_implementation_plan.md). Capacity/integrity часть
Gate A завершена с `proceed_24k`; канонический v2 role contract и metadata-only train selection
на `28 800` строк заморожены. Source materialization извлёк `21 600` RuASD/KSC2 assets и после
exact raw-audio history gate оставил `21 598`. Decode/QA/VAD и audio leakage gate заморозил
`15 000` source rows (`3 × 5 000`) и принял `proceed_20k_balanced`. KK spoof создаётся через
четыре frozen train-only TTS-family; все `7 200` input texts exact-проверены.
Hash-pinned local-only synthesis contract и resumable runner завершили `7 200/7 200` raw WAV
(`4 × 1 800`) без runtime reject. Common audio QA/leakage gate завершён: `6 200` eligible rows,
`5 000` frozen (`4 × 1 250`), exact/near-audio intersections равны нулю. Его immutable receipt
содержит слишком широкое `training_authorized=true`; отдельная reconciliation сохраняет bytes и
разрешает только combined `20 000` manifest и отдельный full training contract. Combined
manifest уже заморожен с `4 × 5 000` cells; contract и no-training preflight завершены без
forward pass, checkpoint или calibration. One-batch tail-unfreeze profile прошёл без OOM и без
artifacts. Единственный write-once training run завершён: tail-unfreeze epoch 2 выбран по macro
RU/KK dev loss `0.08414228`. Metadata-only calibration-input gate также завершён: `81` fresh
VoxForge source identities / `81` distinct contributor groups заморожены after current-history
isolation audit. Новый write-once materialization/audio-isolation gate материализовал `81` source
WAV, retained `79` source-ready, выполнил ровно `79` text-only eSpeak syntheses и заморозил `73`
exact RU pairs после QA/VAD/full-history exact/near-audio screen; calibration/final не запускались.
Подробности:
[v4 KK spoof texts](docs/artifacts/v4/kk_spoof_text_materialization_2026-08-14.md) и
[v4 synthesis plan](docs/artifacts/v4/kk_spoof_synthesis_plan_2026-08-14.md),
[v4 MMS synthesis](docs/artifacts/v4/xlsr_sls_model_v4_kk_spoof_kk_mms_kaz_v1_synthesis_v1.json) и
[v4 KazEmoTTS synthesis](docs/artifacts/v4/xlsr_sls_model_v4_kk_spoof_kk_kazemotts_v1_synthesis_v1.json),
[v4 Piper synthesis](docs/artifacts/v4/xlsr_sls_model_v4_kk_spoof_kk_piper_issai_high_v1_synthesis_v1.json) и
[v4 SparkTTS synthesis](docs/artifacts/v4/xlsr_sls_model_v4_kk_spoof_kk_sparktts_v1_synthesis_v1.json),
[v4 common audio gate](docs/artifacts/v4/kk_spoof_audio_gate_2026-08-15.md) и
[v4 audio-gate reconciliation](docs/artifacts/v4/xlsr_sls_model_v4_kk_spoof_audio_gate_governance_v1.json),
[v4 combined train manifest](docs/artifacts/v4/combined_train_manifest_2026-08-15.md) и
[isolated v4 dev-input receipt](docs/artifacts/v4/isolated_dev_inputs_2026-08-15.md) и
[v4 full training contract](docs/artifacts/v4/v4_training_contract_2026-08-15.md) и
[v4 calibration-input gate](docs/artifacts/v4/calibration_inputs_2026-08-15.md) и
[v4 calibration materialization/isolation](docs/artifacts/v4/calibration_materialization_2026-08-15.md).
Combined dev содержит `1 917` rows; write-once training завершён, calibration/final ещё не
запускались.

## 1. Завершённые этапы

- [x] Создан Python-проект с locked dependencies, CLI, тестами, Ruff и mypy.
- [x] Реализован безопасный audio pipeline: container checks, FFmpeg decode, mono PCM WAV
  16 kHz, QA, WebRTC VAD и окна 64 600 samples.
- [x] Реализованы manifest/asset/license validators, group-aware split и fail-closed research /
  product protocol gates.
- [x] License-ledger CSV gate закреплён fail closed: non-exact header и extra row fields отклоняются,
  шесть legacy `notes` с неэкранированными запятыми исправлены, все `24` строки
  строго загружаются; immutable snapshots не изменены.
- [x] Проверены и задокументированы локальные и внешние RU/KK/mixed источники.
- [x] Собраны B0 baselines и source-mixed stress tests.
- [x] Исправлен RuASD v2 intake; опубликованы write-once selection и rejection receipts.
- [x] Созданы immutable license-ledger snapshots.
- [x] Разделены Stage-A dev, fresh Stage-B dev и calibration roles PyAra.
- [x] Stage A v2 и Stage B v2 обучены на RTX 5060 Ti с CUDA/BF16.
- [x] Выполнен один calibrated confirmatory RU/KK/mixed run с immutable execution lock.
- [x] ToneSpeak проверен как отдельный RU spoof-only research source.
- [x] FastAPI upload/health/readiness scaffold работает fail closed без model score.
- [x] Завершён отдельный Stage-D RU evaluation contract: Dialogs-RU VITS2 fixed Masha/neutral,
  73 frozen Common Voice texts, 55 exact pairs после QA, two-review gate, один preflight и один
  GPU inference run.
- [x] Завершена изолированная v3 ветка: frozen rights/data-governance, label-agnostic
  train-only channel/codec/replay augmentation, Stage-A/Stage-B selection только по разным dev
  loss, отдельная calibration role и один final GPU run на неизменяемых 55 Stage-D парах.
- [x] Полный test suite, Ruff и mypy проходят.
- [x] KazDeepScan v1.0 Research release contract завершён: source/OpenAPI version
  `1.0.0-research`, CLI release identity, неизменные hash-pinned package inputs, полный suite
  без skip, build/install smoke, release receipt и tag `v1.0.0-research`.
- [x] Отдельный local user-audio research inference v1 реализован без изменения frozen runners:
  checksum-pinned Git-ignored B0 checkpoint, external-input guard, обязательный acknowledgment,
  uncalibrated CLI и отдельный `/v1/research/analyze` API без `risk_score`.
- [x] Выполнена ограниченная local-storage cleanup: удалены только caches, exact duplicate
  `ruasd-000000.tar`, неиспользуемый `ffplay` и Italian ML-DF OOD media, непригодные для RU/KK
  v4 train. Старые manifests/reports/write-once contracts, RU/KK sources, TTS weights и
  `/home/ruslan/Downloads/269-lockdown/` сохранены; ни один evaluation не перезапускался.
- [x] XLS-R+SLS model v4 Gate A capacity/integrity: fresh exact audit 250 RuASD TAR, 10 KSC2
  parts и Common Voice history screen; license/model-lock/project-history gate вынес
  `proceed_24k`. Это pre-QA capacity decision, а не готовый train manifest или завершённый
  row-level leakage gate.
- [x] XLS-R+SLS model v4 frozen metadata selection v2: `28 800` unique candidates, точные
  `4 × 7 200` cells, deterministic target/reserve order, нулевой historical sample/text overlap
  и попарно disjoint source/TTS-family role roots.
- [x] XLS-R+SLS model v4 source materialization: `21 600` RuASD/KSC2 files извлечены в новый
  Git-ignored namespace; `21 598` unique raw hashes допущены к decode/QA, две historical
  TeraTTS collisions отклонены.
- [x] XLS-R+SLS model v4 source decode/QA: `18 930` eligible после QA/VAD и exact/near-audio
  screen; frozen source train содержит `15 000` строк (`3 × 5 000`). Решение
  `proceed_20k_balanced`; KK spoof route будет добавлен только после отдельного common gate.
- [x] XLS-R+SLS model v4 KK spoof texts: full KSC2 multipart archive revalidated, `7 200`
  unique frozen transcripts (`4 × 1 800`) exact-извлечены и hash-bound до synthesis.

- [x] XLS-R+SLS model v4 KK spoof synthesis plan: four isolated hash-pinned local TTS routes,
  deterministic profile/seed assignment and append-only resume journal; no synthetic WAV is
  counted as ready before the common audio QA/leakage gate.

- [x] XLS-R+SLS model v4 KK spoof MMS synthesis: `1 800/1 800` local text-only WAVs (`1 500`
  target, `300` reserve), `0` runtime rejects.

- [x] XLS-R+SLS model v4 KK spoof KazEmoTTS synthesis: `1 800/1 800` local text-only WAVs
  (`1 500` target, `300` reserve), `0` runtime rejects.

- [x] XLS-R+SLS model v4 KK spoof Piper synthesis: `1 800/1 800` local text-only WAVs (`1 500`
  target, `300` reserve), `0` runtime rejects.

- [x] XLS-R+SLS model v4 KK spoof SparkTTS synthesis: `1 800/1 800` local text-only WAVs (`1 500`
  target, `300` reserve), `0` runtime rejects.

- [x] XLS-R+SLS model v4 KK spoof common audio gate: все `7 200` WAV прошли один decode/QA/VAD
  и exact/near-audio screen; `6 200` eligible, `1 000` `insufficient_speech` rejects, frozen
  manifest содержит ровно `5 000` spoof rows (`4 × 1 250`) без historical/within-pool
  exact/near collisions. Immutable result содержит слишком широкую формулировку
  `training_authorized=true`; отдельная governance reconciliation сохраняет receipt и запрещает
  actual training до combined manifest и отдельного training contract.

- [x] XLS-R+SLS model v4 combined train manifest: deterministic write-once assembler проверил
  права и SHA-256 всех `20 000` referenced assets, объединил только frozen source `15 000` и
  KK spoof `5 000` в четыре balanced cells по `5 000`. `4 604` KK shared text hashes закреплены
  только внутри train role; на этапе assembly training contract и actual training не создавались.

- [x] XLS-R+SLS model v4 isolated dev inputs: one CUDA execution of the hash-pinned runner
  reused `969` PyAra rows and froze `474` KSC SLR102/Silero V4 pairs (`1 917` combined dev rows).
  KSC source QA retained `571/600`, spoof QA retained `535/571`; only `insufficient_speech`
  rejects occurred, exact/near-audio intersections are zero; этот dev-input run не включал
  training.

- [x] XLS-R+SLS model v4 full training contract: hash-pinned `20 000` four-cell train,
  bilingual `1 917`-row dev, eight-source frozen ledger, XLS-R/runtime/implementation hashes,
  deterministic no-padding sampler and write-once outputs. No-training CUDA preflight verified
  all `21 917` assets with zero train/dev sample/audio/text/group overlap; it did not forward,
  train, calibrate or infer.
- [x] XLS-R+SLS model v4 write-once training run: exact pinned protocol completed in
  `1 869.94` s and selected tail-unfreeze epoch 2 only by macro RU/KK dev loss `0.08414228`.
  Checkpoint is Git-ignored and hash-bound; calibration/final inputs were not read.
- [x] XLS-R+SLS model v4 calibration metadata inputs: current-history isolation audit froze
  `81` fresh VoxForge exact source identities with `81` new contributor groups and verified the
  pinned eSpeak RU route without WAV extraction, synthesis, checkpoint loading, calibration or
  detector inference. Historical VoxForge text overlap is disclosed; v4 train/dev
  sample/text/group overlap is zero. Его required materialization-and-audio-isolation contract
  subsequently completed; calibration remains a separate future checkpoint-scoring contract.
- [x] XLS-R+SLS model v4 calibration materialization/audio isolation: exact archive rebinding
  materialized `81` fresh VoxForge WAV, retained `79` source-ready rows, then generated exactly
  one text-only eSpeak WAV per retained text. QA/VAD and full current-history exact/near screen
  retained `73` complete RU pairs; eight total rejects are `insufficient_speech`, with no
  resynthesis, replacement or backfill. Checkpoint loading, fitting and inference remain false;
  current frozen ledger explicitly prohibits fitting until a new rights decision is recorded.

## 2. Что уже нельзя менять или повторять

- Не перезапускать Stage A v2, Stage B v2, ToneSpeak OOD и research-final v1 по прежним plan.
- Не перезапускать выполненные Stage C и Stage D synthesis, preflight или inference.
- Не перезапускать Stage A v3, Stage B v3, v3 preflight или v3 final inference.
- Не изменять существующие manifests, ledger snapshots, checkpoints, reports и execution locks.
- Не подбирать threshold, temperature, architecture или augmentation по RU/KK/mixed final
  logits и ошибкам.
- Не называть KK `1.0000` product quality: до inference не было двух независимых acoustic
  reviews.
- Не называть mixed result blind project-level final: exact assets ранее видел checkpoint v1.
- Не объединять RU, KK и mixed в одну итоговую accuracy.
- Не подключать текущий research checkpoint к product API risk score. Отдельный user-audio
  contract может возвращать только uncalibrated research signal с false claims.

## 4. Этап B — известный KK evidence gap закрыт

Приоритет: высокий; это проверка качества данных, а не новый final result.

- [x] Сформировать review packet для точных 304 FLEURS/Silero KK assets без model predictions.
- [x] Получить два независимых решения по audibility, соответствию казахскому тексту и
  отсутствию явных дефектов для каждого WAV.
- [x] Подготовить две 304-row формы `reviewer_1` / `reviewer_2` с fail-closed значениями по
  умолчанию и реализовать строгую проверку их связи с packet SHA-256.
- [x] После заполнения форм опубликовать write-once acoustic-gate receipt.
- [x] Не пересчитывать и не повышать статус уже раскрытой метрики `1.0000`.

Packet SHA-256: `1ff70e56f516b09be1dba7b3c3e5c6d890538be076122cf08ae716fada401267`.
Receipt SHA-256: `4147cc8df2658af46c42530ea74e1d9550d5645ab3c176f0462fc93789f47810`.
Обе формы содержат по `304` решений `pass/yes/yes/yes`; predictions и logits в формы не
включались. Строгий gate подтвердил `304/304` exact assets.

Критерий завершения: качество exact KK bytes описано двумя reviews. Результат остаётся
post-inference confirmatory evidence и не становится blind final задним числом.

## 5. Этап C — завершённый blind research suite

Этот historical research suite завершён. Его final errors не используются для выбора v3 или
последующих recipe; следующий новый suite потребует других, действительно unevaluated assets.

### 5.1. Требования к источникам

- новые exact assets, которые ни один checkpoint проекта ещё не оценивал;
- отдельные RU, KK и mixed роли, без pooled score;
- новый точный checkpoint/runtime route, отсутствующий во всех сохранённых manifests;
- architecture-component, family и speaker/voice alias overlaps раскрываются отдельно; из-за
  неполной RuASD metadata не заявлять недоказуемую absolute architecture novelty;
- bona-fide counterpart с максимально сильными правами и group provenance;
- text-only TTS со встроенными публичными voices; reference audio и cloning запрещены;
- exact revision, license, weights/artifact hashes, voice ID и generation parameters;
- acoustic review до inference, а не после него.

VoxForge Russian / Mozilla Data Collective теперь принят как metadata-only RU bona-fide candidate:
exact 3.53-GB GPL-3.0-or-later archive и `6,412` transcript-bound WAV прошли archive-level intake,
а selection заморозил all `81` canonical text groups с distinct conservative contributor groups.
Это ещё не independent binary source: contributor aliases не являются verified speaker IDs.
ToneSpeak остаётся spoof-only и не закрывает bona-fide counterpart сам по себе; YO-CPT и Dusha
также не подходят.

### 5.2. Допустимый практический вариант

Если новый source-level binary release не появится, создать ограниченный research suite из:

- ранее не использованных FLEURS RU/KK bona-fide assets;
- ранее не использованных KSC2 mixed assets;
- нового, предварительно проверенного exact TTS route для каждой языковой роли;
- заранее утверждённых текстовых пар и двух независимых acoustic reviews.

Такой suite может быть asset-level blind, но без нового corpus и verified speaker IDs нельзя
объявлять его полностью source- или speaker-independent.

### 5.3. Порядок выполнения

1. [x] Source/rights review и проверяемый route-level contract. IMS Toucan не выбран; критерий
   абсолютной architecture novelty удалён как недоказуемый по историческим manifests.
2. [x] ISSAI KazakhTTS2 Male2 Tacotron2 + ParallelWaveGAN: artifact lock, outer/inner hashes,
   CRC/config audit, local verified bundle и license-ledger row.
3. [x] Exact-route exposure audit по 46 manifests / 17 657 spoof rows: exact overlap `0`;
   раскрыт overlap 312 строк с тем же Male2 alias через Piper.
4. [x] Frozen technical smoke для KK/RU/mixed: три валидных WAV, detector inference не было;
   packet и две fail-closed listening forms подготовлены.
5. [x] Получить два действительно независимых listening reviews для трёх smoke WAV. Все три
   роли прошли; receipt SHA-256
   `946c3a3a59fdd437553c2fe8e93d4ade157e718cf67505abb1216c02cbc82a73`.
6. [x] Заранее зафиксированная selection policy для `kk`, `ru` и `mixed`: все 173 eligible
   groups, без metric-based selection/backfill. Bona-fide QA оставил 50 RU / 60 KK / 58 mixed.
7. [x] Generated-asset decode/QA/VAD и полный rejection report: 168 generated, 167 ready,
   одна mixed `insufficient_speech`, без regeneration/backfill.
8. [x] Project exposure/leakage против 15 manifests / 11 869 prior rows: exact
   sample/audio/text overlap `0/0/0`; route/voice ограничения раскрыты отдельно.
9. [x] Две full-asset acoustic review формы заполнены до model inference: `334` независимых
   решений, `167/167` exact assets прошли. Gate receipt SHA-256:
   `9a12f235072ce5ae4c3bd6bb0616a804a710abea74f3c3b387cebe12baf8153c`.
10. [x] Новый immutable XLS-R plan с run_id
    `xlsr-sls-stage-b-v2-fresh-suite-stage-c-v1`, output paths и implementation hashes.
11. [x] `--validate-only` на точном implementation tree: `1 310` asset bindings, CUDA BF16.
12. [x] Ровно один GPU inference run: execution-lock SHA-256
    `7d692542e6397c64ac0379eca34564d16f1fd670d5deb93e7a9a940695c2ccdb`.
13. [x] Отчёт с confidence intervals и отдельными RU/KK/mixed метриками опубликован; pooled
    score запрещён, final errors не используются для tuning.

Критерий остановки: если права, generator provenance, project exposure или acoustic gate не
проходят проверку, inference не выполняется.

## 6. Этап D и изолированная v3 ветка завершены

Полный fine-tune или новый head сейчас не обоснованы: главный blocker — независимость и
качество evaluation, а не недостаток train epochs.

### D0. Завершённый RU Dialogs-RU layer

- [x] `73` Common Voice RU `test` bona-fide rows selected без detector/metric output из исходных
  `84`: `5` direct text overlaps с configured roles и вся затронутая group (`11` rows) исключены.
- [x] Новый pinned Dialogs-RU VITS2 route закреплён по revision/files/SHA-256. Wrapper использует
  только `torch.load(weights_only=True)`, fixed `speaker_id=0` (Masha), `emotion_id=0` (neutral)
  и не принимает reference audio или cloning parameters.
- [x] Rights lock допускает только personal research: модель не содержит отдельного `LICENSE`;
  model-card OpenRAIL declaration и pinned Dialogs dataset license не являются коммерческим
  разрешением. Piper, MMS и RHVoice остаются исключёнными historical routes.
- [x] Exact-route audit 53 manifests / 18 422 spoof rows: exact Dialogs route и Masha aliases
  не встречались. Generic `vits2TTS` означает, что architecture-family novelty не заявляется.
- [x] Ровно 73 synthetic WAV созданы из дословно frozen текстов; QA/VAD оставил 55 pair и
  отклонил 18 `insufficient_speech` пар без resynthesis/backfill.
- [x] Две независимые full-asset review формы завершены до inference: все 110 assets прошли
  intelligibility/Russian-text/severe-artifact gate.
- [x] Immutable plan проверил 1 086 calibration/final bindings и выполнил ровно один GPU run.
  RU balanced accuracy `0.9727` (`107/110`); результат не использовать для tuning.

Receipt: [Stage-D Dialogs-RU VITS2 / Masha-neutral](docs/stage_d_dialogs_ru_vits2_intake_2026-08-13.md).
Stage D остаётся immutable v2 evidence. Его exact 55-pair set может быть указан как v3 final
только без изменения, донабора или reselection и только если все решения v3 будут независимо
зафиксированы по train/dev/calibration, без обращения к Stage-D logits и ошибкам v2.

### D1. Выполненный v3 contract

- [x] Locked ledger и governance v2 зафиксировали RuASD train (`1 471`), Stage-A PyAra dev
  (`61`), fresh Stage-B dev (`969`), calibration PyAra (`976`) и тот же Stage-D final (`55`
  pairs); все role intersections по sample/audio/text/group равны нулю.
- [x] До training зафиксированы research scope, rights, Stage-A/Stage-B `dev_loss` selection,
  calibration только для temperature и запрет загрузки Stage-D v2 logits/errors. Первый
  governance v1 отклонён до training из-за неполной формулировки Stage-A selection; v2 исправил
  её на `stage_a_and_stage_b_dev_loss_only`.
- [x] `symmetric-channel-codec-replay-simulation-v1` применяется только на train, симметрично
  bona-fide/spoof и без label в seed derivation; dev, calibration и final не augmented.
- [x] Stage A v3 выбрал epoch 3 (`dev_loss=0.42729345`), Stage B v3 — epoch 4
  (`dev_loss=0.18516177`) на fresh dev. Checkpoint/plan/report paths новые и immutable.
- [x] Новый final plan закрепил тот же 55-pair manifest, two-review/pairing evidence, новые
  output paths, one write-once preflight (`1 086` bindings) и один GPU inference. Итог:
  `107/110`, balanced accuracy `0.972727`, `52/55` полностью верных пар.

v3 final не blind/unseen: exact 55 pairs ранее получили v2 predictions. Это history прямо
раскрыта в contract; v2 logits/errors не были загружены для выбора v3 или evaluation. Старые v2
и новые v3 artifacts остаются immutable.

### D2. Завершённый Common Voice RU / Silero V5.5 `eugene` route

- [x] Новый exact route закреплён: official Silero V5.5 RU `v5_5_ru`, fixed built-in `eugene`,
  `48000` Hz. Package/source/wrapper hashes, ZIP CRC/path/dispatcher allow-list и
  CC-BY-NC-SA-4.0 personal-research scope зафиксированы в
  [Silero V5.5 intake](docs/silero_v5_5_ru_eugene_intake_2026-08-13.md).
- [x] Historical route audit `56` manifests / `18 605` spoof rows: exact V5.5/eugene route
  отсутствует. `1 265` legacy Silero rows означают, что допустим только exact-route claim, без
  architecture-, vendor- или speaker-family independence.
- [x] Full pinned Common Voice RU `test` metadata screen проверил `10 261` records / `2 075`
  client groups против `12 313` configured-role rows и `39 850` rows в `85` manifests. По
  sample/text/parent-group/speaker-pseudo-ID strict whole-group exclusion оставил `6 211`
  records / `1 443` groups; receipt
  `data/manifests/common_voice_ru_v24_full_test_metadata_exposure_screen_v1.json`.
- [x] V5.5 literal-text gate без lexical rewrite проверил все `6 211` metadata survivors:
  `113` direct incompatible records (unsupported quotes/glyph `−`) taint `106` groups; остались
  `5 600` compatible records / `1 337` groups. Receipt
  `data/manifests/common_voice_ru_v24_full_test_silero_v5_5_literal_text_screen_v1.json`.
- [x] Immutable pre-QA selection v1: из `5 600` compatible records / `1 337` groups seed
  `2026-08-13-silero-v5-5-eugene-pre-qa-candidate-v1` выбрал `80` exact records — по одной на
  client group. CSV SHA-256 `73eaf22706419b275517500ebb25973510e8dcccaa94a54f45b4fe2a787f6b50`,
  receipt SHA-256 `a7f0a1b5c3a152c87692e5e9d7d4ac2e02b2b64d9b6933f7f79f29b4e6b6d7ad`; `80/80` sample
  IDs, groups и text hashes уникальны. Размер — pre-QA buffer, не прогноз yield. `55`
  Stage-D/v3 pairs, historical `73` rows и `18` QA rejects не являются резервом/backfill.
- [x] Exact extraction и normal QA/VAD materialized только 80 frozen clip names: `75` ready WAV,
  `5` `insufficient_speech` rejections, без reused/replacement/backfill. Raw manifest SHA-256
  `5543e1e88b688cb79b4401a7ef68ba525d75321cbe98a4837bf7347867a2a9a5`, ready manifest SHA-256
  `2b183adbfcac9b1a6022dd35c2f8b6ec8f111c01b4b3364596c53aff8906192a`, materialization receipt
  SHA-256 `da5f4a79cf0444d0f2e905c4f178760967306d3a393753736e72cc4a27a0da3e`.
- [x] Literal-text binding проверил только 75 ready rows against pinned archive: receipt
  `data/manifests/common_voice_ru_v24_silero_v5_5_eugene_pre_qa_text_binding_v1.json`, SHA-256
  `8418fb0f8725e1408cc7b60886f0e981c90fc255715f147e93c76175ab3978d0`; literal и
  whitespace-only normalized hashes совпали для `75/75` rows, без rewrite, replacement,
  reselection, audio/duration, detector или metric.
- [x] Fixed-profile V5.5/eugene CPU synthesis создал ровно один raw 48 kHz PCM-16 WAV per bound
  text: `75/75` spoof sample IDs/text IDs/text hashes unique; raw manifest SHA-256
  `82b220dbe4663d472a01d9e85e0d7f2309577fe772a0275a27dffeb7102460ae`, synthesis receipt
  SHA-256 `61f5e81712c602fc8ee857f008527878e77a7ccf1e73af277bf3d803c75e9ab6`. Bundle hashes
  reverified before load; no reference audio, cloning, replacement or backfill.
- [x] Normal decode/QA/VAD fully accounted raw spoof layer: из `75` raw WAV retained `42`, все
  `33` rejects — `insufficient_speech`; reused/resynthesis/replacement/backfill `0/false`.
  Ready manifest SHA-256 `60df83502521fb225dbf8af44f80395c6493c9c4fc698a06fdb0c567b74a7036`, QA
  receipt SHA-256 `e9c9fccbba13272cf537e5bad0e9ce70bd2dcd47fbd309bdb4469208453e46db`.
- [x] Immutable pair lock выбрал только matching retained texts: `42` bona fide + `42` spoof
  assets, `33` synthetic QA rejects исключили complete pairs; candidate SHA-256
  `9227455d70c30f6f931902951421abb86b62627513feeb1cfc8530cb38bf2d71`, pairing receipt SHA-256
  `579e32ccf4725e27c4024427e6ca3b29fb6ad759efdccb6558cfc5a348b48d01`.
- [x] 84-asset packet и две completed review forms привязаны к pair-lock WAV hashes: packet
  SHA-256 `3cf55fd48b6bf81ae593661df2a40b4b9e4fb11eb238ca1fce553dea1a3f29f7`; forms with distinct
  pseudonymous IDs have hashes
  `48a3d21eb5c3f580291fa9ea266c70acf578ea2e4ab67c199b2dfafbff6d628d` and
  `645a067c81fa26c1a8d4de7a592bdb5686140c49202685f8fe58a2ac3dd271e3`.
  Fail-closed evaluation passed `84/84` exact assets with two complete
  `pass/yes/yes/yes/yes` decisions; report SHA-256
  `cb9604a6a2c41fa16ce6e0c8c1947e44c0d0d21d626b88ccbf90673c872c3631`. The evaluator confirms
  distinct IDs, not organizational independence.
- [x] Project-exposure audit against all 30 then-current `configs/research/*.json`, their 17
  referenced manifests / 12,313 prior rows found exact `sample_id` / audio SHA-256 / `text_hash`
  overlap `0/0/0`; receipt SHA-256
  `6071deb2f60ca914e475611addf81ef2cf81b485c2b9b86826c5a135c0cca3ff`. It remains a pre-contract
  audit and did not perform or authorize detector inference.
- [x] Write-once immutable XLS-R+SLS Stage-B v2 contract pins this passed gate, project-exposure
  audit, exact route, V2 checkpoint, 976-row PyAra calibration, fixed `0.5` boundary, frozen
  three-source license ledger and fresh output paths. Plan SHA-256
  `cdf3fcbb496006478e575c024963cca497854dae1ce17775e58d95ae4d74cadf`; before preflight it
  permitted neither logits nor a run, and now bounds the completed one-time run.
- [x] One `--validate-only` preflight under the pinned plan validated exactly `1,060` assets
  (`976` calibration + `84` final) and CUDA/BF16 without logits, training, threshold selection or
  detector inference. Receipt SHA-256
  `3df30bd5a70bcb471d57db4a85765658396e6a2491ab9937731018e37e4206f3`.
- [x] Exactly one CUDA/BF16 inference run wrote its lock before logits, then fitted temperature
  only on calibration (`1.29954`) and evaluated the unchanged final layer: `84/84` correct,
  balanced accuracy `1.0000`, `42/42` completely correct pairs. Execution SHA-256
  `0bad2d746c2036068e1c6ae1d160b294ab22a351b3a56f00867825b1fdb28015`; report SHA-256
  `06744ecd71244791efe431c6473b59c8c9962f7b27273ed919495365bbe48991`. Rerun, replacement,
  backfill and tuning on its errors are forbidden.
- [x] The immutable final report inherited `detector_inference_performed=false` from preflight
  despite its execution lock, `status=ok` and 84 final sample results. The report is not mutated
  and inference is not repeated; reconciliation receipt SHA-256
  `0a301c3e4f14d7a5ea048b0cfda7bfae7eda83b6913b787b34d10fc7c079c5b4` binds and discloses the
  status-metadata defect.

## 7. Этап E — API или product track, только по отдельному решению

Локальный research toolkit завершён как KazDeepScan v1.0 Research. Его API scaffold намеренно
остаётся `model_unavailable` без отдельного scorer release. Для product route дополнительно
обязательны:

Post-v1.0 user-audio API является другим ASGI app и не закрывает product gate: он не калиброван,
не публикует risk score, требует acknowledgment и запрещает automatic decisions.

- commercial-clean train/dev/calibration/test data;
- подтверждённые права на ML training, synthetic derivatives и deployment;
- verified speaker/voice groups и независимый binary OOD protocol;
- product calibration и пороги по заданной цене ошибок;
- model/data cards, latency, security, privacy и retention review;
- UX с abstention и без утверждений о личности или мошенничестве.

Только после этого можно реализовать scorer interface, загрузку versioned model release и
перевести `/readyz` из `503`. Streaming, Telegram и телефония идут после надёжного file mode, а
не раньше.

## 8. Рекомендуемый порядок после v3

### 8.1. Evidence policy для будущих layers

- [x] Отказаться от требования «идеального» готового binary dataset и полного перечня каждой
  TTS training utterance как универсального admission gate. Claims теперь ограничиваются
  фактически подтверждёнными dimensions.
- [x] Разделить основной independent layer, external source/generator-family holdout с явным
  `TTS training-data overlap unverified` и same-family sensitivity test. Same-family checkpoint,
  adapter или voice не создаёт новый evidence layer.
- [x] Снизить hard minimum до `60` ready exact pairs при цели `79`. Для multi-speaker source
  сначала максимизировать speaker groups и использовать заранее заданный cap/round-robin;
  single-speaker layer никогда не называть speaker-robust.
- [x] Разрешить cloning-capable model только через fail-closed offline text-only default-voice
  wrapper: no `reference_wav_path` / `prompt_wav_path` / `prompt_text`, no voice-design prefix,
  network, normalizer, denoiser или retry; checkpoint/seed/parameters pinned до WAV.
- [x] Сохранить запреты на использованные human sources/repack, неизвестные voice rights,
  reference/prompt audio, replacement/backfill и post-result tuning.

Полный review: [evidence tiers and VoxCPM2 candidates](docs/external_holdout_policy_and_voxcpm2_candidates_2026-08-14.md).

1. Не повторять Stage-C, Stage-D или v3 run и не использовать их final errors для tuning.
2. Silero V5.5 RU/fixed `eugene` прошёл source/rights/route review; 80-record immutable
   selection, его 75-row QA/VAD-ready bona-fide layer, literal-text binding, 75 raw spoof WAV,
   их 42-row technical-QA retained subset, immutable 42-pair lock, completed `84/84` technical
   acoustic gate, project-exposure audit, immutable contract and единственный CUDA/BF16 run
   закреплены. Не повторять inference, не менять immutable report и не использовать его ошибки
   для tuning; не переиспользовать 55 Stage-D/v3 pairs как blind test.
3. VoxForge Russian прошёл archive-level GPL/transcript intake: `644` submissions,
   `6,412` 48-kHz WAV и `194` source-provided contributor groups; на metadata-only screen raw WAV не извлекались. Full
   metadata screen passed `0/0/0/0/0` sample/two-text/group overlap against 31 configs and 90
   manifests, so all `6,412` records / `194` groups / `81` text groups survive. Frozen
   metadata-only selection then bound all `81` text groups to `81` distinct conservative
   contributor groups, without raw aliases or WAV access. Qwen3-TTS CustomVoice Q8_0 / fixed
   `aiden` route прошёл six-artifact/CUDA lock и exact historical audit: `0` overlap среди
   `18,764` spoof rows в `59` manifests, включая legacy Qwen identifiers и `aiden` aliases. Это
   не Russian-native, speaker- или architecture-independence claim (`aiden` documented as English).
   Frozen materialization/technical QA уже оставил `79/81` ready rows; два `signal_too_quiet`
   rejects recorded without replacement. Literal-text bind ровно этих `79` rows завершён без
   rewrite. Non-candidate CUDA smoke passed. One-shot Qwen synthesis затем создал `79/79` raw
   24-kHz mono WAV с `0` failed attempts; technical synthetic decode/quality/VAD QA retained
   `79/79` normalized 16-kHz mono PCM-16 rows с `0` rejects and no reuse/replacement/backfill.
   Immutable exact `text_hash`/`text_id` lock затем зафиксировал `79` pairs / `158` assets без
   metric selection. Full `158`-asset packet и две independent completed forms прошли fail-closed
   acoustic/language gate: `158/158` assets получили два `pass/yes/yes/yes/yes` решения; report
   SHA-256 `bf7a6d84c7ecb71462c128b70f68d8f939ebece916fc11e87c6b9ac8afd26029` и detector
   inference не выполнялся. Current project-exposure audit затем закрепил `33` configs / `18`
   referenced manifests / `12,397` prior rows и `0/0/0` sample/audio/text overlap; receipt
   SHA-256 `8696bfbea8d9f59451881bcf6ee875ff235c2e281c7b9ddbe5be4ecf74804a72` также не
   разрешает inference. Separate immutable Stage-B v2 evaluation contract затем закрепил exact
   candidate/gates, 976-row PyAra calibration, selected checkpoint, fixed `0.5` boundary,
   three-source ledger and new write-once paths; plan SHA-256
   `9e36b5d6a35cfa0b796ff24e62f3bfa78667d0b1d9da993f1863a2fe61c421cc`. Его единственный
   no-logit preflight на `1,134` assets успешно завершён с receipt SHA-256
   `4f9a56ab8de8fdb876d64c408032c468492c5ca813a34ad6bd846dd543312e5b`, без training,
   threshold selection или inference. Единственный lock-before-logits run затем fitted
   temperature только на PyAra (`1.299543`) и дал `146/158`, balanced accuracy `0.9241`,
   bona-fide/spoof recall `74/79` и `72/79`, fully correct pairs `67/79`. Execution/report hashes:
   `286c4e680defb01217b138da604096d29a7615d632bab18cff7653ac867e3c94` /
   `7da4df67f756addbc4bcd21868e294a62a150985479f30aad7e8f93bdbc96dff`. Не повторять run,
   не менять immutable artifacts и не использовать final errors для tuning/replacement.
   UtrobinTTS остаётся rejected из-за `76` historical unversioned rows.
4. Denis 1.0 source/frozen-selection/bona-fide-QA gate завершён: exact `109,594,943` bytes / SHA-256
   `75e2c63c5082df7623c6a98c529718b22015dfbd2d38a1ea328635f4dd4ccf9b`, безопасные gzip/TAR,
   `1,150` exact pairs и full decodes, `1,143` rows `>=2.5` s, zero direct sample/audio/text
   overlap. Current v2 screen закрепил `35` configs / `95` manifests. Это conditional external
   holdout, не speaker-independent: source single-speaker, а полный screen нашёл `12` unique
   historical `ru_RU-denis-medium` samples (`11` train, `1`
   dev). Provider duration table больше не требуется, если duration воспроизводимо derived из
   byte-pinned audio; остальные запреты не ослаблены. Exact upstream VoxCPM2 whitespace collapse
   разрешён только как заранее bound literal/canonical transform; semantic rewrite запрещён.
   Seeded metadata-only target заморозил ровно `79` rows с category balance `27/26/26`, без
   duration/audio-quality/model selection. Exact extraction и normal decode/QA/VAD оставили
   `64` ready (`23/17/24`) и отклонили `15` `insufficient_speech` (`4/9/2`); target 79 не
   достигнут, minimum 60 пройден, reuse/replacement/backfill запрещены.
5. Official OpenBMB VoxCPM2 artifact/source/history gate завершён: revision `bffb3d…`, source
   commit `ee8161…`, `9` model files / `4,960,731,703` bytes, safetensors, AudioVAE
   weights-only state, tokenizer/source и narrow wrapper проверены. Full history из `95`
   manifests / `40,682` rows содержит `0` VoxCPM rows: generator family новый, но training-data
   overlap unverified. Official frozen `uv.lock` установлен в isolated Python 3.12. Первый
   launcher получил versioned duplicate-`streaming` failure до upstream generation/WAV; после
   минимального wrapper correction выполнен ровно один actual CUDA call в `bwrap --unshare-net`:
   `0` network attempts, no reference/prompt/LoRA/normalizer/denoiser/retry, `48 kHz` mono output.
   Smoke не повторять. Отдельный immutable contract уже закрепил exact `64` ready texts,
   literal/collapse-whitespace/NFKC hashes, один model load, ровно одну attempt на row, frozen
   runtime/program hashes и будущие write-once paths; receipt SHA-256
   `943a9595968996f29da1a13f213e28419fc2c7b5215df790e4d4c440528f2b7b`. Contract исполнен
   ровно один раз: `64/64` raw WAV, `0` failures/network attempts/retries, затем один
   normal synthetic decode/QA/VAD pass без reuse. Он сохранил `53`, отклонил `11`
   `insufficient_speech` и закрепил `stop_below_minimum_60`. Pair lock, reviews, exposure
   и detector inference для этого route не выполнять; rejects не пересинтезировать
   и не backfill. Новый evaluation layer требует отдельного genuinely new source/route и
   нового pre-outcome contract.
6. MCSKL остаётся blocked до archive-level license resolution (`CC BY` против
   `CC BY-NC-SA`, `78` против `73` participants). VoxCPM2-KZ-Darwin дополнительно требует
   provenance/base/LoRA audit и после RU VoxCPM2 может быть только same-family sensitivity test.
7. Если нужен product/API route — остановиться до отдельного commercial-rights, privacy,
   verified-speaker, product-calibration и deployment contract.

Не начинать AASIST, новый full fine-tune или API score только ради повышения этой метрики: это не
исправит главный риск — отсутствие независимого, ещё не оценивавшегося final source.

## 9. Проверка каждого следующего этапа

После изменения кода обязательны:

```bash
.venv/bin/ruff check src tests scripts services
.venv/bin/mypy src scripts services
.venv/bin/pytest -q
git diff --check
```

Каждый завершённый эксперимент дополнительно требует pinned inputs, asset validation,
write-once outputs, отдельный Markdown receipt и отдельный Git commit.

## 10. KazDeepScan v1.0 Research release

- [x] Текущий personal-research scope заморожен без новых ML runs и post-result tuning.
- [x] Source release и OpenAPI синхронизированы на `1.0.0-research`; CLI показывает
  `KazDeepScan v1.0 Research`. Python distribution metadata `0.1.0`, `pyproject.toml` и
  `uv.lock` не изменены, поскольку их SHA-256 закреплены completed write-once plans.
- [x] `pyarrow==22.0.0` закреплён exact-hash Linux test overlay; ToneSpeak tests выполняются, не
  skip, без изменения historical lockfile.
- [x] На момент release полный suite содержал `307` successful items; Ruff/mypy проходили.
- [x] Wheel и sdist собираются; wheel install smoke проходит в пустом temporary environment.
- [x] README, API docs, status, changelog и release contract согласованы.
- [x] Release не включает raw audio, weights или scorer и сохраняет fail-closed API boundary.
- [x] Versioned release receipt опубликован, atomic commit помечен tag `v1.0.0-research` и
  отправлен в configured GitHub `origin`.

## 11. Post-v1.0 local user-audio research inference v1

- [x] Новый contract имеет purpose `research_user_audio_only` и не ссылается на evaluation
  runners/outputs.
- [x] Exact B0 checkpoint закреплён file SHA-256 `7b620af0…0539` и state SHA-256
  `a586c323…24de`; loader использует `torch.load(weights_only=True)` и strict state load.
- [x] Входы ниже `data/`, `models/`, `artifacts/`, `checkpoints/` блокируются CLI; API принимает
  только temporary upload после `acknowledge_research_only=true`.
- [x] Decode/QA/VAD и 64 600-sample windows выполняются по отдельному contract; scoring идёт на
  CPU без fitting, calibration, threshold selection или записи output artifacts.
- [x] CLI/API возвращают raw logit, `uncalibrated_spoof_score`, fixed-zero-boundary
  interpretation, per-window details и обязательное предупреждение; `risk_score` отсутствует.
- [x] Existing `services.api.main:app`, frozen manifests, reports, execution locks и evaluation
  results не изменены и не перезапущены.
- [x] На момент завершения этого этапа одиннадцать новых contract/engine/CLI/API tests и полный
  suite `318 passed` подтверждали gates.

Следующий user-inference model/contract, data source, scorer/product work или evaluation
оформлять новой versioned v1.x/v2 development; опубликованные write-once receipts не менять и
пользовательские inference outcomes не использовать для tuning.
